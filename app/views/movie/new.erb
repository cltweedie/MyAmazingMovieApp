<h1>Add a Movie</h1>

<% form_for( Movie.new, url_for( :movies, :new ),
            class: "form-group col-md-7", :method => :post ) do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title, class: "form-control" %>

  <%= f.label :year %>
  <%= f.text_field :year, class: "form-control" %>

  <%= f.label :description %>
  <%= f.text_area :description, class: "form-control", rows: 5 %>

  <%= f.label :poster %>
  <%= f.text_field :poster, class: "form-control" %>

  <%= f.label :runtime %>
  <%= f.text_field :runtime, class: "form-control" %>

  <%= f.submit :save, class: "btn btn-primary" %>

<% end %>

<div class="col-md-4 col-md-offset-1 jumbotron">
  <p>Please enter a few details about the movie using the form on the left (all fields are required) and we'll add it to our database.</p>
</div>
